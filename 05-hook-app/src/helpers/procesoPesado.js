

const procesoPesado = (iteraciones) => {
    for (let index = 0; index < iteraciones; index++) {
        console.log('ahí vamos')
        
    }

    return `${iteraciones} iteraciones realizadas `;
}

export {procesoPesado};